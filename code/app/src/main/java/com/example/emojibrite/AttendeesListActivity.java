package com.example.emojibrite;

import android.content.Intent;
import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

public class AttendeesListActivity extends AppCompatActivity {
    // 2 buttons

    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_attendees_list);

//         get the data passed from event details
//        Intent intent = getIntent();
//          retrieve the arraylist of attendees from the intent
//        String Uid = intent.getStringExtra("Uid");

        // the arraylist to the fragment


    }
}
